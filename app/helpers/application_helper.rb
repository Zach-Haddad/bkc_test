module ApplicationHelper
  # make sure devise instatiates new resource(user) each time modal opens
  def resource_name
    :user
  end

  def resource
    @resource ||= User.new
  end

  def devise_mapping
    @devise_mapping ||= Devise.mappings[:user]
  end
end
